{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "proxy:build": "bash scripts/build-proxy.sh all",
    "proxy:build:current": "bash scripts/build-proxy.sh current",
    "proxy:build:mac": "bash scripts/build-proxy.sh mac",
    "proxy:build:mac-arm": "bash scripts/build-proxy.sh mac-arm",
    "proxy:build:mac-intel": "bash scripts/build-proxy.sh mac-intel",
    "proxy:build:windows": "bash scripts/build-proxy.sh windows",
    "proxy:build:linux": "bash scripts/build-proxy.sh linux",
    "test:proxy": "node --test proxy/*.test.js"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-clipboard-manager": "^2.3.2",
    "@tauri-apps/plugin-opener": "^2.5.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.11"
  }
}
